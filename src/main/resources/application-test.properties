# Base de donnees en memoire (H2) pour les tests
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=false

# Cache Caffeine simple pour les tests
spring.cache.type=simple

# Creation automatique des tables
spring.jpa.hibernate.ddl-auto=create-drop

# On desactive les envois d'emails reels pendant les tests
spring.mail.host=localhost

# Cle secrete factice obligatoire pour que Spring Security accepte de demarrer
loto.security.remember-key=CleDeDevSuperSimple123
spring.mail.username=tonycoloricchio01@gmail.com
spring.mail.password=spupqbtuiobzazir

# ==========================================
# CONFIGURATION DE TEST (Contournement V1)
# ==========================================

# 1. On desactive Flyway pour les tests (puisque le V1 manque)
spring.flyway.enabled=false
